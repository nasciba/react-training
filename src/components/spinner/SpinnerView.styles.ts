import { makeStyles } from '@material-ui/core/styles';

export const spinnerStyle = makeStyles({
    root: {
        height: '100%',
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center'
    }
})