import { makeStyles} from '@material-ui/core';

const buttonStyle = makeStyles({
    root: {
        margin: 10,
        borderRadius: 5,
        fontWeight:'bold'

    },
})

export default buttonStyle